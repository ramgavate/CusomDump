# mongo-sampler-dump

Export a sample copy of your MongoDB database (N random docs per collection), suitable for importing into a free-tier Atlas cluster.

## Install

```bash
git clone <this-repo>
cd mongo-sampler-dump
npm install
